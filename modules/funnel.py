import pandas as pd

def compute_funnel_metrics(df: pd.DataFrame) -> pd.DataFrame:
    """
    Define steps and compute:
      - conversion rates
      - time-to-convert distributions
      - cohort retention curves
    """
    # TODO: implement
    results = pd.DataFrame()
    return results
